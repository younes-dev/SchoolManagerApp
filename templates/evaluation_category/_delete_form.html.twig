<form method="post" action="{{ path('evaluation_category_delete', {'id': evaluation_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evaluation_category.id) }}">
    <button class="btn">Delete</button>
</form>
