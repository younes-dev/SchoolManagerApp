{% extends 'baseAdmin.html.twig' %}
{% import 'macros/macros_heading.html.twig' as macros %}

{% block title %}Classe{% endblock %}

{% block body %}

    {% if macros.panel_heading is defined %}
        {{ macros.panel_heading("Show Classe") }}
    {% endif %}
<div class="container-fluid">

<table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classe.name }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ classe.year }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('classe_index') }}">back to list</a>

    <a href="{{ path('classe_edit', {'id': classe.id}) }}">edit</a>

    {{ include('classe/_delete_form.html.twig') }}

</div>
{% endblock %}
