{% extends 'baseAdmin.html.twig' %}
{% import 'macros/macros_heading.html.twig' as macros %}

{% block title %}EvaluationCategory{% endblock %}

{% block body %}
{% if macros.panel_heading is defined %}
    {{ macros.panel_heading("Evaluation Category") }}
{% endif %}
<div class="container">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation_category.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ evaluation_category.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evaluation_category_index') }}">back to list</a>

    <a href="{{ path('evaluation_category_edit', {'id': evaluation_category.id}) }}">edit</a>

    {{ include('evaluation_category/_delete_form.html.twig') }}

    </div>
{% endblock %}
